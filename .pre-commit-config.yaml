# Secret scanning at commit time (heritage workspace standard).
# One-time setup per clone: `uv tool install pre-commit && pre-commit install`
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.24.3
    hooks:
      - id: gitleaks
